<div class="max-w-4xl mx-auto p-4">
  <div class="mb-6">
    <h1 class="text-2xl font-bold">New Reply</h1>
    <p class="text-gray-600">Replying to: <%= @topic.title %></p>
  </div>

  <%= render "form", reply: @reply %>

  <div class="mt-6">
    <%= link_to "Back to Topic", topic_path(@topic), class: "text-blue-600 hover:text-blue-800" %>
  </div>
</div>
