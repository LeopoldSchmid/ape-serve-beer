<div class="bg-white shadow rounded-lg p-6">
  <div class="flex justify-between items-start mb-4">
    <div>
      <h2 class="text-xl font-bold text-gray-900"><%= topic.title %></h2>
      <p class="text-sm text-gray-500">
        Posted by <%= topic.author_name %> • <%= time_ago_in_words(topic.created_at) %> ago
      </p>
    </div>
    <div class="flex space-x-2">
      <%= link_to 'Edit', edit_topic_path(topic), class: 'text-blue-600 hover:text-blue-800' %>
      <%= link_to 'Delete', topic_path(topic), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'text-red-600 hover:text-red-800' %>
    </div>
  </div>

  <div class="prose max-w-none">
    <%= topic.content %>
  </div>
</div>
